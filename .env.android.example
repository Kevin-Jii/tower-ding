# Android 打包专用环境变量（复制为 .env.android 后按需修改）
# 构建命令：pnpm build:android 或 taro build --type h5 --mode android

# 后端识别 Android App 客户端（写入 X-Client-Source 请求头）
TARO_APP_CLIENT_SOURCE=android-app

# 可选：覆盖 API 根路径（默认见 config/index.ts）
# TARO_APP_API_BASE_URL=https://tower.usove.online/api/v1
